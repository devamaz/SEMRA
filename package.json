{
  "name": "semra",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SEMRA — landing site, prayer PWA, admin, and push notifications for Suncity Estate Mosque",
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "web-push": "^3.6.0"
  }
}
