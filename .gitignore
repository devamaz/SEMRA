# Dependencies
node_modules/

# macOS
.DS_Store

# Logs
*.log

# pi agent local config
.pi/

# Runtime / PII state — never commit live push endpoints or the send log
data/subscriptions.json
data/adhan-sent.json

# Admin-uploaded academy gallery photos — live media, not source
data/media/
docs/